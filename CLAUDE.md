# הנחיות לעבודה בריפו הזה

שני חלקים: **א׳ הנחיות כלליות · ב׳ הנחיות לפרויקט.**

---

## חלק א׳ — הנחיות כלליות

1. **עברית תכליתית וברורה** — ענה כך שגם מי שאין לו רקע בתכנות ואנגלית יבין; בלי מונחים טכניים או מילים באנגלית ללא הסבר קצר לצידם.
2. **הסבר לפני פעולה** — לכל שינוי: מה המצב לפני, מה אחרי, יתרונות/חסרונות — ורק אז לבצע.
3. **עבודה על main** — עבוד ישירות על הענף הראשי (main); דחוף/מזג מיד בסיום, בלי אישור מיזוג נפרד.
4. **תמציתיות בכתיבת הנחיות** — כל הנחיה בכל מסמך נכתבת מתומצתת ותכליתית: המינימום שסוכן AI צריך כדי לפעול נכון, בלי הרחבות, חזרות או דוגמאות מיותרות.
5. **מיגרציות, פריסה ומיזוג — אוטומטית, בלי לבקש אישור.** בסיום שינוי: להריץ את המיגרציות על המסד, לפרוס את פונקציות השרת ואת האתר, ולמזג — ברצף, בלי לעצור לשאול. לדווח מה נעשה בסיכום. הפירוט: `docs/04-מדריך-הקמה.md`, "פריסה".

### כלל אנונימיות
**אין לכתוב בשום קובץ בריפו שם של אדם, של פרויקט אחר או של ארגון · כתובת מייל · דומיין · מזהה חשבון · מפתח.** לא בקוד, לא בהערות, לא בתיעוד ולא בהודעות קומיט. הכלל חל גם על פרויקטים אחרים ולא רק על זה.

**שני חריגים, ואין אחרים:**
1. **כתובת האתר הציבורי** של הכלי הזה — מותרת, כי כל מטרתה שאנשים ייכנסו.
2. **כתובת המייל של מנהל המערכת** — מותרת אם באמת נדרשת. עדיף בכל זאת להימנע ולהשתמש ב-`ADDRESS` כמציין מקום.

כל ערך אחר מגיע ממשתני סביבה או מטבלת ההגדרות במסד. התיעוד מסביר **איפה** להגדיר ערך, לעולם לא **מהו**. כשצריך להדגים ערך — משתמשים בשם בדוי שאינו קיים במציאות.

**לפני פרסום או שיתוף הריפו** מריצים סריקה: כתובות מייל, דומיינים, מזהי UUID, מפתחות, ושמות פרויקטים אחרים — גם בקוד, גם בתיעוד וגם בהיסטוריית הגרסאות.

### תהליך העבודה בסשן
- **פתיחת סשן:** `git pull` מלא מ-main (להתחיל מהגרסה העדכנית); לבדוק מה כבר נעשה/בעבודה לאחרונה — לא לעבוד פעמיים על אותו דבר.
- **במהלך העבודה:** בעבודה ארוכה — לשמור תוצרי ביניים לקבצים (לא רק בזיכרון), כך שעצירה באמצע לא מאבדת כלום.
- **סיום סשן:** לפני `push` — למשוך שוב שינויים חדשים מ-main, לפתור התנגשויות (למטה), ואז commit + push/מיזוג מיד.

### התנהגות בהתנגשויות
- **התנגשות אמיתית** (שני סשנים שינו **בדיוק את אותו דבר** בצורה סותרת — אותה שורה/הגדרה/ערך) → **לעצור**, להסביר בעברית פשוטה מה מתנגש ובאיזה קובץ, ולבקש הכרעת בעלים. לא לפתור לבד.
- **כל שאר ההתנגשויות** (עריכות מקבילות שאינן סותרות) → לפתור לבד תוך **שמירת שני הצדדים**, ולהודיע בסיכום מה סונכרן.

### יצירת תמונות
> 🔴 **אזהרת עלות: זה לא חינם.** מודלי התמונה מסומנים בתמחור הרשמי כ-"Free tier:
> Not available", ומפתח המשויך לחשבון עם חיוב **מחויב על כל קריאה** — כמה סנטים
> לתמונה. **אין להריץ יצירת תמונות בסדרות בלי אישור בעלים.**

> 🔴 **אזהרת פרטיות: תיאור התמונה נשלח לשרת חיצוני.** לכן **אסור** שיופיע בו
> שום נתון אמיתי: שם של אדם, של ארגון או של פרויקט · כתובת מייל · טלפון ·
> כתובת אתר או דומיין · תוכן ממסד הנתונים · צילום מסך של נתונים אמיתיים ·
> מזהה חשבון או מפתח. מתארים רק סצנה כללית ("שני אנשים ברכב, בוקר, סגנון
> מינימליסטי"). אם צריך להדגים משהו מהמוצר — משתמשים בשמות בדויים.

הקריאה נעשית ישירות מול ה-API, בכל סשן שיש בו את משתנה הסביבה `GEMINI_API_KEY`.
המפתח נקרא מהסביבה ולעולם לא נכתב בקוד או בתיעוד:
```bash
curl -s -X POST "https://generativelanguage.googleapis.com/v1beta/models/gemini-2.5-flash-image:generateContent?key=$GEMINI_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{"contents":[{"parts":[{"text":"<תיאור כללי, בלי שום נתון אמיתי>"}]}]}' \
  | jq -r '.candidates[0].content.parts[] | select(.inlineData) | .inlineData.data' | base64 -d > output.png
```

---

## חלק ב׳ — הנחיות לפרויקט

### הכלי בקצרה
ניהול נסיעות משותפות בין עמיתים לעבודה. שלושה תפקידים: נהג, נוסע, מנהל.
לפני שנוגעים בקוד — לקרוא את `docs/01-אפיון-מוצר.md`, ובמיוחד את שלוש
מכונות המצבים (נסיעה, הרשמה, תשלום). כל ההתנהגות נגזרת מהן.

### כללים שאסור לשבור

**1. פעולות שמשנות סטטוס עוברות דרך פונקציה במסד הנתונים, לא דרך עדכון ישיר.**
`request_booking`, `decide_booking`, `cancel_booking`, `close_ride`,
`mark_payment`, `confirm_payment` וכו׳. שם נאכפים הכללים: מקומות פנויים,
מדיניות ביטול, יצירת חיובים, התראות ותיעוד. עדכון ישיר מהדפדפן עוקף אותם.

**2. אין להסיר את טריגרי השמירה.**
`members_guard`, `members_guard_delete`, `bookings_guard_insert`,
`bookings_guard_update`, `payments_guard_insert`, `payments_guard_update`.
הם מונעים שתי פרצות שנמצאו בבדיקה בפועל: נוסע שהופך את עצמו למנהל, ומשלם
שמאשר לעצמו תשלום. הגנת השורות לבדה לא מספיקה, כי היא לא מגינה על עמודות.

**3. אין לתת ל-`anon` הרשאה על אף טבלה.** בלי כניסה, אפס גישה לנתונים. זו
ההבטחה למשתמשים. החריג היחיד הוא `public_stats()` — פונקציה שלא מקבלת קלט
ומחזירה ארבעה סכומים על כלל המערכת, בלי שום פילוח לאדם או לארגון. פונקציה
נוספת שנחשפת ל-`anon` מחייבת את אותו רף: בלי קלט, בלי פירוט, ובלי דרך
להסיק ממנה על אדם מסוים.

**4. אין ליצור מדיניות כתיבה ישירה על `charges`.** חיובים נוצרים רק דרך פונקציות.

**5. מפתחות וכתובות נשמרים ב-`app_config` ובמשתני סביבה בלבד** — לא בקוד ולא בהיסטוריית הגרסאות.

**6. פרטי קשר נשארים ב-`member_contacts`** ולא עוברים ל-`members`. ההפרדה
היא מה שמאפשר לקיים את ההבטחה "טלפון רק למי שנוסע איתך".

**7. הזהות היא חשבון האימות (`members.user_id`), לא השם ולא קוד הארגון.**
`login_key` הוא שריד מהמודל הישן ואין להשתמש בו. שם התצוגה ניתן לשינוי בידי
בעליו, וקוד הארגון נדרש רק ב-`join_org`, לא בכניסה.

**8. חבר שממתין לאישור חייב לראות את השורה של עצמו.** בלי זה הוא נופל למבוי
סתום: הכלי לא מזהה אותו כמשויך, ההצטרפות נכשלת ב"כבר משויך", ואין יציאה.

**9. לכל פונקציית `admin_*` יש בדיקת `is_platform_admin()` בשורה הראשונה.**
הן `security definer`, ובלי הבדיקה כל אחת מהן היא דלת אחורית לכל המסד.
למנהל מערכת אין ואסור שתהיה הרשאת קריאה ישירה על `platform_admins`,
`platform_admin_emails`, `user_activity` ו-`suspended_users`. הסתרת כפתור
בממשק אינה הגנה.

**10. אין להסיר את בדיקת ההשהיה** מ-`current_member_id`, `current_org_id`
ו-`is_manager`. שם היא נאכפת על כל הטבלאות בבת אחת, במקום בעשרות מדיניות.

### שפה עיצובית
מוגדרת כולה ב-`src/index.css`. שלושה כללים שאסור לשבור:
פינה אחת לכל תפקיד (תגית=עגולה · כפתור ושדה=12px · כרטיס=16px · מגירה=22px) ·
מבטא אחד בלבד (ירוק אורן, בלי צבע מבטא שני) · היררכיה ממשקל וצבע, לא מגודל.
אייקונים: `@phosphor-icons/react` בלבד, בלי אימוג׳י בממשק ובלי SVG ידני.
הפונט מתארח ב-`public/fonts/` ואינו נטען מרשת חיצונית.

### מלכודת בבנייה
בסביבות מסוימות מוגדר `VITE_SUPABASE_URL` של פרויקט אחר, ו-Vite קולט אותו
בשקט. לכן אין ערך ברירת מחדל בקוד: כשההגדרה חסרה הכלי נעצר בטעינה עם
הודעה ברורה, במקום להצביע בשקט על מסד נתונים לא נכון.
אחרי כל בנייה כדאי לוודא:
```bash
grep -o 'https://[a-z0-9]*\.supabase\.co' dist/assets/*.js | sort -u
# חייב להחזיר בדיוק את הכתובת שהוגדרה במשתני הסביבה, ולא אחרת
```

### בדיקות
Chromium בסביבת הענן חסום מרשת חיצונית. לבדיקת ממשק מול מסד הנתונים האמיתי
מקימים שרת מתווך מקומי על 127.0.0.1 ובונים עם `VITE_SUPABASE_URL` שמצביע
אליו. הפירוט ב-`docs/04-מדריך-הקמה.md`.

לפני סיום כל שינוי משמעותי:
```bash
npx tsc -b --noEmit && npm run build
```

### פריסה
נעשית בסיום כל שינוי בלי לבקש אישור (הנחיה 5). שלושה חלקים לפי מה שהשתנה:
מיגרציה על המסד · פונקציית שרת · בניית האתר ופריסתו.

שלוש נקודות שחוזרות ליפול: לוודא **לאיזה פרויקט** רצה המיגרציה (יש יותר
מאחד בחשבון) · לשמר `verify_jwt: false` בפונקציות השרת · לוודא ש-
`.env.production` קיים מקומית לפני פריסת האתר, אחרת הבנייה יוצאת בלי חיבור
למסד. אחרי הפריסה בודקים על החבילה החיה שהיא מצביעה על המסד הנכון.
הפירוט: `docs/04-מדריך-הקמה.md`, "פריסה".

### מה לא נבנה בכוונה
ראו `docs/02-רעיונות-ושדרוגים.md`, סעיף "רעיונות שנשקלו ונדחו": מפה בזמן
אמת, צ'אט פנימי, תשלום בכרטיס אשראי ודירוג כוכבים נדחו מסיבות מנומקות.
לא להוסיף אותם בלי לדון מחדש.
